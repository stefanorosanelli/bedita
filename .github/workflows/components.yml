name: 'Publish components'

on:
  push:
    branches:
      - '**'
    paths:
      - 'plugins/BEdita/**'
      - '.github/workflows/*'
    tags:
      - '*'

jobs:
  filter-tree:
    uses: ./.github/workflows/filter-components.yml
